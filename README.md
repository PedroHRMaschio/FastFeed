
---

# **FastFeed**

A Python + FastAPI social-media backend featuring authentication, photo uploads, data validation, a relational database and a real-time feed of posts from other users.
Inspired by the first versions of Instagram — built for study, practice and portfolio presentation.

---

## 🚀 Features

* User authentication (register/login)
* Post photos with captions
* View a feed with posts from other accounts
* Database-backed storage
* Data validation with FastAPI & Pydantic

---

## 🛠 Tech Stack

| Technology             | Purpose                                       |
| ---------------------- | --------------------------------------------- |
| **Python**             | Backend language                              |
| **FastAPI**            | Web framework & API layer                     |
| **Pydantic**           | Data validation and schema modeling           |
| **SQL/NoSQL Database** | Persistent storage for users, posts & follows |
| **GitHub**             | Version control & portfolio hosting           |
| **ImageKit**           | Image storage and delivery                    |

---

## 📦 Installation

Clone this repository:

```bash
git clone https://github.com/PedroHRMaschio/FastFeed.git
cd FastFeed
```

Create a virtual environment:

```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

Install dependencies:

```bash
pip install -r requirements.txt
```

---

## ▶ Running the Project

Start the API server:

```bash
uvicorn app.main:app --reload
```

Access documentation automatically generated by FastAPI:

```
http://localhost:8000/docs
```

---

## 📂 Project Structure (example)

```
FastFeed/
│── app/
│   ├── main.py
│   ├── models/
│   ├── routes/
│   ├── database.py
│   └── schemas/
│
├── media/            # optional - stores user uploaded images
├── .gitignore
├── README.md
└── requirements.txt
```

---

## 🗺 Roadmap

* [ ] Add JWT authentication
* [ ] Enable image storage (local / S3 / Cloud)
* [ ] Build UI for user interaction
* [ ] Add comments + likes system
* [ ] Add notifications feed

---

## 📜 License

This project is open-source and free to use.

---
